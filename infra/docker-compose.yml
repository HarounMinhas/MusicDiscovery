version: '3.9'
services:
  api:
    build: ../apps/api
    environment:
      DATA_MODE: tokenless
      PORT: 8080
    ports:
      - '8080:8080'
  web:
    build: ../apps/web
    environment:
      VITE_API_BASE: http://localhost:8080/api
    ports:
      - '5173:4173'
