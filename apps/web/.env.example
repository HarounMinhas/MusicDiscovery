# API route prefix exposed by the backend
VITE_API_PREFIX=/api

# Full backend origin used by production/static deployments without a reverse proxy
# (example Render deployment)
VITE_API_BASE_URL=https://harounminhas-github-io.onrender.com
